{
  "compilerOptions": {
    /* --- MOTOR NATIVO (TS 6.0/7.0 Go-Engine Ready) --- */
    "target": "ES2025",              // Alvo específico para 2026 (suporte a Temporal e RegExp.escape)
    "lib": ["dom", "dom.iterable", "esnext", "es2025"], // Libs atualizadas para APIs de 2025/2026
    "module": "NodeNext",            // Recomendado no lugar de ESNext para melhor suporte a pacotes ESM puros
    "moduleResolution": "NodeNext",  // NodeNext é o padrão ouro no TS 6.0 para projetos Next.js 16
    "verbatimModuleSyntax": true,   
    "isolatedDeclarations": true,    // NOVO E OBRIGATÓRIO: Permite que o motor Go gere tipos em paralelo
    "moduleDetection": "force",
    "noEmit": true,

    /* --- RIGOR DE MISSÃO CRÍTICA (Zero Runtime Errors) --- */
    "strict": true,
    "noUncheckedIndexedAccess": true, 
    "exactOptionalPropertyTypes": true, 
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false, 
    "noUncheckedSideEffectImports": true, 

    /* --- PERFORMANCE: EXPLICIT TYPES (TS 6.0 Speedup) --- */
    /* No TS 6.0, 'types' vazio [] é o novo padrão para evitar carregar o lixo do node_modules */
    "types": ["node", "react", "react-dom"], 
    "stableTypeOrdering": true,      // NOVO: Garante que a ordem dos tipos seja determinística no motor nativo

    /* --- NEXT.JS 16 & REACT 19 ARCHITECTURE --- */
    "jsx": "react-jsx",
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsbuildinfo",
    
    /* --- INTEROP & SECURITY --- */
    "esModuleInterop": true,         // Nota: TS 6.0 agora assume true por padrão, mas mantemos para clareza
    "resolveJsonModule": true,
    "allowJs": true,
    "skipLibCheck": true,          
    "forceConsistentCasingInFileNames": true,

    /* --- INFRASTRUCTURE PATHS (2026 Subpath Style) --- */
    "paths": {
      "@/*": ["./src/*"],
      "#/*": ["./src/*"]            // Adição de Subpath Imports para paridade com o package.json
    },

    /* --- DEBUGGING & AUDIT --- */
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts"
  ],

  "exclude": [
    "node_modules", 
    ".next", 
    "out",
    "build"
  ]
}
