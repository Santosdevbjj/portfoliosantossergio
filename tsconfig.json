{
  "compilerOptions": {
    /* ---------------------------------------------------------------------- */
    /* CORE TARGET & RUNTIME (Node.js 24 + 2026 Standards)                    */
    /* ---------------------------------------------------------------------- */
    "target": "ESNext",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],

    /* ---------------------------------------------------------------------- */
    /* STRICTNESS (Rigor de Missão Crítica)                                   */
    /* ---------------------------------------------------------------------- */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "forceConsistentCasingInFileNames": true,
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false,

    /* ---------------------------------------------------------------------- */
    /* EMIT & MODULE RESOLUTION (Next.js 16 + Motion 12 Compat)               */
    /* ---------------------------------------------------------------------- */
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "Bundler", // Essencial para reconhecer subcaminhos como 'motion/react'
    "moduleDetection": "force",    // Garante que todo arquivo seja tratado como módulo
    "isolatedModules": true,
    "jsx": "react-jsx",

    /* ---------------------------------------------------------------------- */
    /* INTEROP & MODERN JS                                                    */
    /* ---------------------------------------------------------------------- */
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "skipLibCheck": true,          // Vital para ignorar inconsistências de tipos em pacotes externos

    /* ---------------------------------------------------------------------- */
    /* PROJECT STRUCTURE                                                      */
    /* ---------------------------------------------------------------------- */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    /* ---------------------------------------------------------------------- */
    /* PERFORMANCE & CACHE (Ajustado para Termux)                             */
    /* ---------------------------------------------------------------------- */
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsbuildinfo",

    /* ---------------------------------------------------------------------- */
    /* NEXT.JS PLUGIN                                                         */
    /* ---------------------------------------------------------------------- */
    "plugins": [
      {
        "name": "next"
      }
    ]
  },

 "paths": {
      "@/*": ["./src/*"]
    }
  
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "src/proxy.tsx", 
    "src/dictionaries/*.json"
  ],

  "exclude": ["node_modules"]
}
