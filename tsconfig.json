{
  "compilerOptions": {
    /* --- MOTOR NATIVO TS 6.0 (BRIDGE TO TS 7.0) --- */
    "target": "ES2025",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "ESNext",
    /* 'Bundler' é o padrão ouro para Next.js 16 e Turbopack */
    "moduleResolution": "Bundler",
    "moduleDetection": "force",

    /* --- COMPLIANCE TS 6.0 (SOLUÇÃO DO ERRO) --- */
    /* Removido 'baseUrl' e 'rootDir' para total compatibilidade com o deploy da Vercel */
    "noEmit": true,
    "verbatimModuleSyntax": true,
    "isolatedModules": true,

    /* --- RIGOR DE MISSÃO CRÍTICA (NÍVEL SENIOR) --- */
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false,

    /* --- NEXT.JS 16 & REACT 19 ARCHITECTURE --- */
    "jsx": "react-jsx",
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsconfig.tsbuildinfo",

    /* --- INTEROP & SECURITY --- */
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,

    /* --- PERFORMANCE & TYPES (TS 6.0 FIX) --- */
    "types": ["node"],

    /* --- INFRASTRUCTURE PATHS (TS 6.0 COMPLIANCE) --- */
    /* Sem baseUrl, os caminhos devem começar com './' para serem relativos à raiz */
    "paths": {
      "@/*": ["./src/*"],
      "#/*": ["./src/*"]
    },

    "plugins": [
      {
        "name": "next"
      }
    ]
  },

  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "src/**/*",
    "next.config.mjs"
  ],

  "exclude": [
    "node_modules",
    ".next",
    "out",
    "dist",
    "build"
  ]
}
