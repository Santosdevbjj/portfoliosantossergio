{
  "compilerOptions": {
    /* --- MOTOR NATIVO TS 6.0 & NEXT.JS 16 --- */
    "target": "ESNext",
    "lib": ["dom", "dom.iterable", "esnext"],
    "noEmit": true,
    "module": "ESNext",
    /* 'Bundler' é o padrão ouro para Next.js 16 e resolve o Octokit corretamente */
    "moduleResolution": "Bundler", 
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    
    /* --- RIGOR DE MISSÃO CRÍTICA (Padrão Bancário) --- */
    "strict": true,
    "noUncheckedIndexedAccess": true, 
    "exactOptionalPropertyTypes": true, 
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false, 

    /* --- NEXT.JS 16 & REACT 19 ARCHITECTURE --- */
    "jsx": "react-jsx",
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsconfig.tsbuildinfo",
    
    /* --- INTEROP & SECURITY --- */
    /* Importante para que o Octokit (CJS/ESM) seja importado sem erros */
    "esModuleInterop": true,         
    "resolveJsonModule": true,
    "allowJs": true, 
    "skipLibCheck": true, /* Essencial para evitar erros em tipos de terceiros no deploy */
    "forceConsistentCasingInFileNames": true,

    /* --- PERFORMANCE & TYPES --- */
    "types": ["node"],

    /* --- INFRASTRUCTURE PATHS (TS 6.0 Compliance) --- */
    "paths": {
      "@/*": ["./src/*"],
      "#/*": ["./src/*"]
    },

    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "src/**/*",
    "next.config.ts",
    "tests/**/*.ts"
  ],

  "exclude": [
    "node_modules", 
    ".next", 
    "out",
    "dist",
    "build"
  ]
}
