{
  "compilerOptions": {
    /* ---------------------------------------------------------------------- */
    /* CORE TARGET & RUNTIME (Node.js 24 + 2026 Standards)                    */
    /* ---------------------------------------------------------------------- */
    "target": "ESNext",
    "lib": ["DOM", "DOM.Iterable", "ESNext"],

    /* ---------------------------------------------------------------------- */
    /* STRICTNESS (Rigor de Missão Crítica)                                   */
    /* ---------------------------------------------------------------------- */
    "strict": true,
    "noUncheckedIndexedAccess": true, // Essencial para lidar com dicionários dinâmicos
    "forceConsistentCasingInFileNames": true,
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,            // Limpeza de código em produção
    "noUnusedParameters": false,       // Mantido falso para permitir assinaturas de Proxy/Middleware

    /* ---------------------------------------------------------------------- */
    /* EMIT & MODULE RESOLUTION (Next.js 16 + Bundler Resolution)             */
    /* ---------------------------------------------------------------------- */
    "noEmit": true,
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "isolatedModules": true,
    "jsx": "react-jsx",

    /* ---------------------------------------------------------------------- */
    /* INTEROP & MODERN JS                                                    */
    /* ---------------------------------------------------------------------- */
    "esModuleInterop": true,
    "resolveJsonModule": true,         // Vital para src/dictionaries/*.json
    "allowJs": true,
    "skipLibCheck": true,              // Performance no build (ignora erros em node_modules)

    /* ---------------------------------------------------------------------- */
    /* PROJECT STRUCTURE                                                      */
    /* ---------------------------------------------------------------------- */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },

    /* ---------------------------------------------------------------------- */
    /* PERFORMANCE & CACHE                                                    */
    /* ---------------------------------------------------------------------- */
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsbuildinfo", // Organização de cache

    /* ---------------------------------------------------------------------- */
    /* NEXT.JS PLUGIN                                                         */
    /* ---------------------------------------------------------------------- */
    "plugins": [
      {
        "name": "next"
      }
    ]
  },

  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "src/proxy.ts" 
  ],

  "exclude": ["node_modules"]
}
