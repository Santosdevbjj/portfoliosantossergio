{
  "compilerOptions": {
    /* --- BRIDGE TO TS 6.0 / TSGO ENGINE OPTIMIZATION --- */
    "target": "ESNext", 
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "ESNext",
    "moduleResolution": "Bundler", // OBRIGATÓRIO para TS 6.0 (deprecia node10)
    "verbatimModuleSyntax": true,   // Garante paridade com compiladores nativos (Go/Rust)
    "isolatedModules": true,         
    "moduleDetection": "force",
    "noEmit": true,

    /* --- RIGOR DE MISSÃO CRÍTICA (Zero Runtime Errors) --- */
    "strict": true,
    "noUncheckedIndexedAccess": true, 
    "exactOptionalPropertyTypes": true, 
    "noImplicitOverride": true,
    "useUnknownInCatchVariables": true,
    "noUnusedLocals": true,
    "noUnusedParameters": false, // Mantido false para flexibilidade em handlers
    "noUncheckedSideEffectImports": true, // TS 5.9+

    /* --- PERFORMANCE: EXPLICIT TYPES (TS 6.0 Speedup) --- */
    /* Evita o carregamento automático de todo o @types de node_modules */
    "types": ["node", "react", "react-dom"], 

    /* --- NEXT.JS 16 & REACT 19 ARCHITECTURE --- */
    "jsx": "react-jsx",
    "incremental": true,
    "tsBuildInfoFile": "node_modules/.cache/tsbuildinfo",
    
    /* --- INTEROP & SECURITY --- */
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "allowJs": true,
    "skipLibCheck": true, // Importante para não travar em tipos de terceiros legados          
    "forceConsistentCasingInFileNames": true,

    /* --- INFRASTRUCTURE PATHS (TS 6.0 Style) --- */
    /* Removido baseUrl para evitar depreciação. O paths agora usa caminhos relativos ao arquivo. */
    "paths": {
      "@/*": ["./src/*"]
    },

    /* --- DEBUGGING & AUDIT --- */
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    "plugins": [
      {
        "name": "next"
      }
    ]
  },
  
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "src/types/**/*.d.ts"
  ],

  "exclude": [
    "node_modules", 
    ".next", 
    "out",
    "build",
    "**/*.spec.ts", // Exclui testes do build principal para performance
    "**/*.test.ts"
  ]
}
