import MdxLayout from '@/components/mdx-layout'
import { getServerDictionary } from "@/lib/getServerDictionary"
import { normalizeLocale } from "@/dictionaries/locales"

export const dynamic = 'force-static';

export default async function Page(props) {
  const params = await props.params;
  const lang = params.lang;
  const dict = await getServerDictionary(lang);
  const safeLang = normalizeLocale(lang);

  // Extraímos os dados do dicionário para variáveis simples
  // Isso evita erros de parsing do Acorn dentro do JSX do MDX
  const article = dict.articles.items[0];
  const title = article?.title || "Artigo";
  const description = article?.description || "";

  return (
    <MdxLayout lang={safeLang} dict={dict}>
      
      # {title}

      > {description}

      ## Por que usar MDX manual?

      O uso de MDX permite unir a simplicidade do Markdown com o poder do **React 19**. No contexto do seu portfólio, isso garante:

      - **Controle Total**: Integre componentes diretamente no texto.
      - **Performance Máxima**: O Next.js 16 pré-renderiza este conteúdo no servidor.
      - **SEO Nativo**: Totalmente compatível com os metadados do seu dicionário.

      ---

      ## Exemplo de Código

      Abaixo, um exemplo de implementação consistente com o **Node 24** e o novo motor do **Tailwind 4.2**:

      ```javascript
      // Exemplo de log de sistema em Node 24
      console.log("Sistema operando em Node 24 com Vercel iad1");
      ```

      ### Conclusão

      Este artigo está totalmente integrado ao seu sistema de **i18n** (Português, Inglês e Espanhol) e utiliza o `TableOfContents` para gerar o sumário automaticamente a partir destes títulos.

    </MdxLayout>
  );
}
